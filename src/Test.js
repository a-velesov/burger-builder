import React from 'react';

export const Test = ({ props1, props2 }) => { // ...props?

  console.log(props1);
  console.log(props2);

  /// ??????? ///

  const prevPage = () => {
    props.history.push('/')
  }



  this.props

  return (
    <>
      <button onClick={() => prevPage()} />
    </>
  );
};

<Test />
